{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "properties": {
        "env": {
            "type": "string",
            "enum": ["dev", "dev-config", "production"]
        },
        "gConfigPath": {
            "type": "string",
            "pattern": "^[^/].+?(?=\\.json)\\.json$"
        }
    }
}